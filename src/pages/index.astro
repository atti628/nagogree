---
import Layout from '../layouts/Layout.astro';
import { getConcerts, getAbout } from "../library/microcms";

// 演奏会と、連絡先(about)の両方を取得
const concertResponse = await getConcerts({ limit: 10 });
const about = await getAbout();

const allConcerts = concertResponse.contents;
const today = new Date().toISOString();
const upcomingConcerts = allConcerts.filter(concert => concert.date >= today).reverse().slice(0, 3);
---

<Layout title="ホーム">

  <div class="fixed inset-0 z-[-1] bg-cover bg-center bg-no-repeat"
       style="background-image: url('https://placehold.jp/3d4070/ffffff/1920x1080.png?text=Background%20Image');">
    <div class="absolute inset-0 bg-black/40"></div>
  </div>

  <section class="h-screen flex flex-col justify-center items-center text-center px-4">
    <h1 class="text-[#228B22] text-5xl md:text-7xl font-black tracking-widest mb-6 animate-on-scroll opacity-0 translate-y-10 transition-all duration-1000 drop-shadow-[0_0_15px_rgba(255,255,255,1)] bg-white/20 backdrop-blur-sm p-4 rounded-xl border border-white/50">
      NAGO<br>GREE
    </h1>
    <p class="text-white text-lg md:text-2xl font-bold tracking-[0.2em] animate-on-scroll opacity-0 translate-y-10 transition-all duration-1000 delay-300 drop-shadow-[0_0_5px_rgba(0,0,0,0.8)]">
      響け、私たちのハーモニー
    </p>
  </section>

  <div class="space-y-16 pb-24">

    <section class="max-w-4xl mx-auto bg-[#FDFBF7]/90 backdrop-blur-sm p-8 md:p-16 shadow-lg rounded-sm animate-on-scroll opacity-0 translate-y-10 transition-all duration-1000">
      <div class="text-center mb-10">
        <h2 class="text-[#228B22] text-3xl font-black tracking-widest mb-2">ABOUT US</h2>
        <span class="text-sm text-gray-500 tracking-wider">団紹介</span>
      </div>
      
      <div class="flex flex-col md:flex-row items-center gap-10">
        <div class="w-full md:w-1/2 aspect-video bg-gray-300 flex items-center justify-center text-gray-500">
          写真が入ります
        </div>
        <div class="w-full md:w-1/2 space-y-6">
          <p class="leading-loose text-gray-800">
            私たちNagoGreeは、○○を拠点に活動する合唱団です。<br>
            「歌う喜び、届ける感動」をモットーに、日々練習に励んでいます。<br>
            個性豊かなメンバーが織りなすハーモニーをお楽しみください。
          </p>
          <div class="text-right">
            <a href="/about" class="inline-block border-2 border-[#228B22] text-[#228B22] font-bold py-3 px-8 hover:bg-[#228B22] hover:text-white transition-colors duration-300 tracking-widest">
              詳しくはこちら
            </a>
          </div>
        </div>
      </div>
    </section>

    <section class="max-w-4xl mx-auto bg-[#FDFBF7]/90 backdrop-blur-sm p-8 md:p-16 shadow-lg rounded-sm animate-on-scroll opacity-0 translate-y-10 transition-all duration-1000">
      <div class="text-center mb-10">
        <h2 class="text-[#228B22] text-3xl font-black tracking-widest mb-2">CONCERT</h2>
        <span class="text-sm text-gray-500 tracking-wider">演奏会情報</span>
      </div>

      <ul class="space-y-4 mb-10">
        {upcomingConcerts.length === 0 ? (
          <li class="text-center text-gray-500 py-4">現在、予定されている演奏会はありません</li>
        ) : (
          upcomingConcerts.map((concert) => (
            <li class="border-b border-[#DDCBB4] pb-4">
              <a href={`/concert/${concert.id}`} class="block hover:opacity-60 transition-opacity flex flex-col md:flex-row md:items-center gap-2 md:gap-6">
                <time class="text-sm text-[#228B22] font-bold font-mono min-w-[120px]">
                  {new Date(concert.date).toLocaleDateString("ja-JP", { year: 'numeric', month: '2-digit', day: '2-digit', weekday: 'short' })}
                </time>
                <h3 class="text-lg font-bold text-gray-800">{concert.title}</h3>
                <span class="md:ml-auto text-[10px] bg-[#228B22] text-white px-2 py-1 rounded-full">COMING SOON</span>
              </a>
            </li>
          ))
        )}
      </ul>

      <div class="text-center">
        <a href="/concert" class="inline-block border-2 border-[#228B22] text-[#228B22] font-bold py-3 px-8 hover:bg-[#228B22] hover:text-white transition-colors duration-300 tracking-widest">
          演奏会情報一覧へ
        </a>
      </div>
    </section>

    <section class="max-w-4xl mx-auto bg-[#FDFBF7]/90 backdrop-blur-sm p-8 md:p-16 shadow-lg rounded-sm animate-on-scroll opacity-0 translate-y-10 transition-all duration-1000">
      <div class="text-center mb-10">
        <h2 class="text-[#228B22] text-3xl font-black tracking-widest mb-2">ACTIVITY</h2>
        <span class="text-sm text-gray-500 tracking-wider">活動報告</span>
      </div>

      <div class="flex flex-col md:flex-row-reverse items-center gap-10">
        <div class="w-full md:w-1/2 aspect-video bg-gray-300 flex items-center justify-center text-gray-500">
          活動風景の写真
        </div>
        <div class="w-full md:w-1/2 space-y-6">
          <p class="leading-loose text-gray-800">
            日々の練習の様子や、合宿、イベントなどの活動記録をブログ形式でお届けします。<br>
            NagoGreeの普段の雰囲気を感じてください。
          </p>
          <div class="text-left">
            <a href="/activity" class="inline-block border-2 border-[#228B22] text-[#228B22] font-bold py-3 px-8 hover:bg-[#228B22] hover:text-white transition-colors duration-300 tracking-widest">
              活動日誌を見る
            </a>
          </div>
        </div>
      </div>
    </section>

    <section class="max-w-4xl mx-auto bg-[#FDFBF7]/90 backdrop-blur-sm p-8 md:p-16 shadow-lg rounded-sm animate-on-scroll opacity-0 translate-y-10 transition-all duration-1000">
      <div class="text-center mb-10">
        <h2 class="text-[#228B22] text-3xl font-black tracking-widest mb-2">JOIN US</h2>
        <span class="text-sm text-gray-500 tracking-wider">入団希望の方へ</span>
      </div>

      <div class="text-center space-y-8">
        <p class="leading-loose text-gray-800">
          私たちと一緒に歌いませんか？<br>
          経験者・未経験者問わず、音楽が好きな仲間を募集しています。<br>
          見学・体験はいつでも大歓迎です！
        </p>
        <a href="/join" class="inline-block bg-[#228B22] text-white font-bold py-4 px-12 shadow-lg hover:bg-[#1a6b1a] hover:shadow-xl hover:-translate-y-1 transition-all duration-300 tracking-widest">
          詳しくはこちら
        </a>
      </div>
    </section>

    <section class="max-w-4xl mx-auto bg-[#FDFBF7]/90 backdrop-blur-sm p-8 md:p-16 shadow-lg rounded-sm animate-on-scroll opacity-0 translate-y-10 transition-all duration-1000">
      <div class="text-center mb-10">
        <h2 class="text-[#228B22] text-3xl font-black tracking-widest mb-2">SNS</h2>
        <span class="text-sm text-gray-500 tracking-wider">公式SNS</span>
      </div>

      <div class="flex justify-center gap-6">
        {about.twitter && (
          <a href={about.twitter} target="_blank" rel="noopener noreferrer" class="w-16 h-16 bg-black text-white rounded-full flex items-center justify-center hover:scale-110 transition-transform duration-300">
            <span class="font-bold text-xl">X</span>
          </a>
        )}
        
        {about.instagram && (
          <a href={about.instagram} target="_blank" rel="noopener noreferrer" class="w-16 h-16 bg-gradient-to-tr from-yellow-400 via-red-500 to-purple-500 text-white rounded-full flex items-center justify-center hover:scale-110 transition-transform duration-300">
            <span class="font-bold text-xl">In</span>
          </a>
        )}

        {about.youtube && (
          <a href={about.youtube} target="_blank" rel="noopener noreferrer" class="w-16 h-16 bg-[#FF0000] text-white rounded-full flex items-center justify-center hover:scale-110 transition-transform duration-300">
            <svg xmlns="http://www.w3.org/2000/svg" class="w-8 h-8" fill="currentColor" viewBox="0 0 24 24">
              <path d="M19.615 3.184c-3.604-.246-11.631-.245-15.23 0-3.897.266-4.356 2.62-4.385 8.816.029 6.185.484 8.549 4.385 8.816 3.6.245 11.626.246 15.23 0 3.897-.266 4.356-2.62 4.385-8.816-.029-6.185-.484-8.549-4.385-8.816zm-10.615 12.816v-8l8 3.993-8 4.007z"/>
            </svg>
          </a>
        )}
      </div>
      
      <div class="text-center mt-8">
        <p class="text-gray-600 text-sm">演奏動画や活動の様子を更新中！</p>
      </div>
    </section>

  </div>

</Layout>